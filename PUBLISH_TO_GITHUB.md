# Publish to GitHub

This bundle is ready to be committed to a repository named `openclaw-pilot`.

## What still needs to happen

Create an empty GitHub repository named:

- `openclaw-pilot`

Then upload these files or push them with git.

## Suggested commands

```bash
git init
git branch -M main
git add .
git commit -m "Initial public repo content for OpenClaw Pilot"
git remote add origin git@github.com:<your-username>/openclaw-pilot.git
git push -u origin main
```

## Suggested repository settings

- Visibility: public
- Default branch: `main`
- Enable Issues: yes
- Enable Discussions: optional
- Enable Actions: yes
- Add topics:
  - openclaw
  - ai-agent
  - prompt-engineering
  - workflow
  - task-compiler
  - agentic-workflows
